- name: installing nginx and run playbook
  hosts: web
  become: yes
  tasks:
    - name: installing ngnix
      ansible.builtin.package:
        name: nginx
        state: present
    - name: running the ngnix
      ansible.builtin.service:
        name: nginx
        state: started